/*
create array for the starting Y values of each row
rowHeight = [25, 75, 125, 175, 225, 275;
create array denoting number of invaders
invaderCount = [];

function setup() {
  rectMode(CENTER);
  createCanvas(500, 500);
  for loop to spawn invaders
  for (let invaderSpawned = 0; invaderSpawned < 15; invaderSpawned++){
    let 
  }
}

function draw() {
  background(0);
}

class defender
constructor {
give variables for x, y, speed, and shots fired
}

class invader
constructor {
give variables for x, y, row, and speed
}
movement {
make invaders in the same row move in unison, lowering down by a bit and reversing direction when one hits the edge of the screen
invaders should speed up as more of them are destroyed
}
hit {
invader dies on hti and all other invaders get faster
}
show {
show different sprites depending on an invader's starting row
}
*/
